{
  "//": "Generated by Copilot (GPT-4.1) on 2025-06-30 14:01 PDT. This file defines the JSON schema for modules in copilot-instructions-builder.",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ModuleSchema",
  "type": "object",
  "required": [
    "id",
    "name",
    "type",
    "version",
    "priority",
    "metadata",
    "instructions"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9\\-_]+$",
      "description": "Unique identifier for the module"
    },
    "name": {
      "type": "string",
      "description": "Human-readable module name"
    },
    "type": {
      "type": "string",
      "enum": ["base", "domain", "task"],
      "description": "Module type"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semantic version"
    },
    "dependencies": {
      "type": "array",
      "items": { "type": "string" },
      "description": "IDs of required modules",
      "default": []
    },
    "conflicts": {
      "type": "array",
      "items": { "type": "string" },
      "description": "IDs of conflicting modules",
      "default": []
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Tags for searching/filtering",
      "default": []
    },
    "priority": {
      "type": "number",
      "enum": [1.0, 1.2, 1.5],
      "description": "Module priority (base=1.0, domain=1.2, task=1.5)"
    },
    "metadata": {
      "type": "object",
      "required": ["description", "author", "category", "weight"],
      "properties": {
        "description": { "type": "string" },
        "author": { "type": "string" },
        "category": {
          "type": "string",
          "enum": ["frontend", "backend", "testing", "devops", "general"]
        },
        "weight": { "type": "number" }
      }
    },
    "variables": {
      "type": "object",
      "patternProperties": {
        "^[A-Z0-9_]+$": {
          "type": ["string", "number", "boolean", "object", "array", "null"]
        }
      },
      "description": "Template variables for substitution",
      "default": {}
    },
    "instructions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "content", "merge_strategy"],
        "properties": {
          "id": { "type": "string" },
          "content": { "type": "string" },
          "merge_strategy": {
            "type": "string",
            "enum": ["replace", "append", "prepend", "smart_merge"]
          },
          "conditions": {
            "type": "object",
            "properties": {
              "include_if": { "type": "string" },
              "exclude_if": { "type": "string" },
              "require_modules": {
                "type": "array",
                "items": { "type": "string" }
              },
              "conflict_with": {
                "type": "array",
                "items": { "type": "string" }
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    },
    "hooks": {
      "type": "object",
      "properties": {
        "pre_compose": {
          "type": "array",
          "items": { "type": "string" }
        },
        "post_compose": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "additionalProperties": false,
      "default": {}
    }
  },
  "additionalProperties": false
}
